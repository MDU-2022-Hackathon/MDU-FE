import { Box, Center } from "@chakra-ui/react";
import type { NextComponentType } from "next";

const UserProfileText: NextComponentType = () => {
    return (
        <Box>
            <Center>User Profile Text</Center>
        </Box>
    );
};

export default UserProfileText;
