import { Box, Center } from "@chakra-ui/react";
import type { NextComponentType } from "next";

const UserProfileSkill: NextComponentType = () => {
    return (
        <Box>
            <Center>User Profile Skill Stack</Center>
        </Box>
    );
};

export default UserProfileSkill;